<view class="container">
  <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}};" bindscrolltolower="lower">
    <view style='padding: 0 30rpx;'>
    <count-list wx:for="{{list}}" wx:key="{{index}}" count-list="{{item}}" ></count-list>
    </view>
  </scroll-view>
</view>